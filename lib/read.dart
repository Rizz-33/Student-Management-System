import 'package:flutter/material.dart';

class ReadDetails extends StatelessWidget {
  const ReadDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Student Details"),
      ),
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}